---
extends: default

rules:
  line-length:
    max: 120
    level: warning
  truthy:
    allowed-values: ['true', 'false', 'on']
  document-start: enable
  indentation:
    spaces: 2
    indent-sequences: true
